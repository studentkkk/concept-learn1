# series-contract.md

> 本文件是 **python-basic-course** 系列课程的契约（Series Contract）。
> 它规定了本系列的目录结构、文件命名规则与内容组织方式。
> 任何新增单元、新增课程文件，都必须遵守本契约。

---

## 1. 系列概述

| 项目 | 内容 |
| --- | --- |
| 系列名称 | Python 基础语法课程 |
| 项目文件夹 | `python-basic-course` |
| 单元数量 | 4 个 |
| 单元顺序 | 固定，不可调整 |
| 面向对象 | 零基础 Python 初学者 |
| 学习载体 | Jupyter Notebook（`.ipynb`） |
| 首页入口 | `index.html` |

**单元顺序（固定）：**

1. 单元 1 —— Python 运行环境
2. 单元 2 —— Notebook 与内核
3. 单元 3 —— 报错与 Traceback
4. 单元 4 —— 变量与基本类型

---

## 2. 目录结构（完整树状结构）

```text
python-basic-course/
├─ index.html
├─ series-contract.md
├─ _series/
│  └─ concepts.md
└─ 01.ipynb
```

### 2.1 各项说明

| 路径 | 类型 | 作用 |
| --- | --- | --- |
| `index.html` | 文件 | 课程首页，提供 4 个单元的导航入口 |
| `series-contract.md` | 文件 | 课程契约：目录结构、命名规则、内容规范 |
| `_series/` | 目录 | 系列级共享资料目录（下划线前缀表示"非课程正文"） |
| `_series/concepts.md` | 文件 | 全部单元、全部课程的知识点清单 |
| `01.ipynb` | 文件 | 配套学生练习笔记本（由讲师/学生自行创建与填写，本契约只登记其命名与位置） |

> 说明：`01.ipynb` 属于**学生练习笔记本**，不承担讲义内容。它位于项目根目录，与 `index.html` 同级。

---

## 3. 文件命名规则

### 3.1 总则

1. **全部文件名使用小写英文**，不得出现大写字母。
2. 单词之间用**连字符** `-` 分隔（kebab-case），不得使用空格、下划线或中文。
3. 文件名只允许出现：小写字母 `a-z`、数字 `0-9`、连字符 `-`、点号 `.`（仅用于分隔扩展名）。
4. 扩展名必须小写：`.html`、`.md`、`.ipynb`。
5. 名称需语义明确，禁止 `new`、`final`、`test1` 这类无意义命名。

### 3.2 根目录文件

| 规则 | 示例 |
| --- | --- |
| 首页固定为 `index.html` | `index.html` |
| 契约固定为 `series-contract.md` | `series-contract.md` |
| 练习笔记本用**两位数字**编号 | `01.ipynb`、`02.ipynb` |

### 3.3 系列资料目录 `_series/`

| 规则 | 示例 |
| --- | --- |
| 目录以单个下划线 `_` 开头 | `_series/` |
| 系列级知识点总表固定为 `concepts.md` | `_series/concepts.md` |
| 新增系列级资料同样使用 kebab-case | `_series/glossary.md` |

### 3.4 单元与课程文件命名

- 单元标识：`unit` + 两位数字，如 `unit1`、`unit2`、`unit3`、`unit4`。
- 课程（lesson）标识：`lesson` + 两位数字，如 `lesson01`、`lesson02`。
- 组合方式：`unit{单元号}-lesson{课号}-{英文短标题}.{扩展名}`
- 示例：`unit1-lesson01-python-install.md`

> 当前阶段项目只落地契约、知识点总表、首页与练习笔记本；上表规则用于后续扩展，扩展时必须遵守。

### 3.5 命名反例（禁止）

| 禁止写法 | 问题 | 应改为 |
| --- | --- | --- |
| `Index.html` | 含大写 | `index.html` |
| `series_contract.md` | 用了下划线 | `series-contract.md` |
| `课程契约.md` | 中文文件名 | `series-contract.md` |
| `01 练习.ipynb` | 含空格与中文 | `01.ipynb` |
| `py-1.md` | 编号非两位 | `unit1-lesson01-....md` |

---

## 4. 内容组织规范

### 4.1 单元 ↔ 知识点映射

`_series/concepts.md` 是**唯一的知识点权威清单**，必须覆盖 4 个单元的全部课程，逐条列出。

内容层级固定为：

```text
单元 → 课程 → 知识点
```

### 4.2 每课结构（讲义统一模板）

每一课按以下顺序书写：

1. 学习目标
2. 知识点讲解（对应 `concepts.md` 中该课的条目）
3. 示例代码
4. 常见误区
5. 练习（指向 `01.ipynb` 对应小节）

### 4.3 `index.html` 要求

- 是 4 个单元的统一导航入口，按固定顺序排列。
- 每个单元显示：单元编号、单元标题、一句话简介、进入链接。
- 页面内不得出现 4 个单元以外的顺序性入口。

---

## 5. 运行环境

本系列在以下环境中验证通过（记录时间：2026-09-23）。

### 5.1 环境参数

| 项目 | 值 |
| --- | --- |
| 操作系统 | Windows |
| Python 解释器 | CPython 3.12.10（64 位） |
| 解释器路径 | `C:\Users\Lenovo\AppData\Local\Programs\Python\Python312\python.exe` |
| 已注册内核 | `python3` → `...\Python312\share\jupyter\kernels\python3` |
| ipykernel | 7.3.0 |
| 编辑器 | VS Code 1.138.0（安装于 `E:\vscode`） |
| 必需扩展 | `ms-python.python`、`ms-toolsai.jupyter` |

### 5.2 环境约束

1. 统一使用 Python 3，不使用 Python 2。
2. 不使用 Anaconda / Miniconda，依赖统一通过 `pip` 管理。
3. 内核选择：打开 `01.ipynb` 后，在右上角内核选择器中选择 **Python 3.12.10**。
4. 若内核列表中同时出现多个解释器，一律选择上述 3.12.10 路径对应的那一项。
5. 本契约第 2 节的目录树**只登记课程内容文件**。`.venv/`、`.vscode/`、`requirements.txt` 等环境脚手架不属于课程内容；如需启用，必须先在本节登记后方可创建。

---

## 6. 变更规则

1. 4 个单元的顺序**不得调整、不得重排、不得删减**。
2. 新增文件必须满足第 3 节命名规则，并同步更新本契约的目录树。
3. 新增知识点必须同步更新 `_series/concepts.md`。
4. 变更本契约需在文件末尾记录修改说明。

---

## 7. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-09-23 | 初版：确定目录结构、命名规则与 4 个单元顺序 |
| 2026-09-23 | 新增第 5 节「运行环境」，登记解释器、内核与编辑器参数；原第 5、6 节顺延为第 6、7 节 |
